# Руководство по внесению вклада в GHouse

Спасибо за интерес к проекту GHouse! Это руководство поможет вам внести свой вклад в развитие системы управления автономным агрокомплексом.

## 📋 Содержание

- [Как начать](#-как-начать)
- [Правила кода](#-правила-кода)
- [Структура коммитов](#-структура-коммитов)
- [Pull Request](#-pull-request)
- [Тестирование](#-тестирование)

## 🚀 Как начать

### 1. Форк репозитория

Нажмите кнопку **Fork** на GitHub для создания копии проекта в вашем аккаунте.

### 2. Клонирование

```bash
git clone https://github.com/YOUR_USERNAME/ghouse.git
cd ghouse
```

### 3. Настройка окружения

```bash
# Backend
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt

# Frontend (опционально)
cd ../ghouse-dashboard
# Frontend работает напрямую в браузере
```

### 4. Создание ветки

```bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/issue-123
```

## 📝 Правила кода

### Python (Backend)

- Следуйте [PEP 8](https://pep8.org/)
- Используйте type hints
- Добавляйте docstrings для публичных функций
- Максимальная длина строки: 100 символов

```python
def calculate_temperature(target: float, current: float) -> float:
    """
    Рассчитывает дельту температуры.
    
    Args:
        target: Целевая температура
        current: Текущая температура
    
    Returns:
        Разница температур
    """
    return target - current
```

### JavaScript (Frontend)

- Используйте ES6+ синтаксис
- Избегайте var, используйте const/let
- Добавляйте JSDoc для сложных функций

```javascript
/**
 * Обновляет данные датчиков воздуха
 * @param {Object} data - Данные с датчиков
 * @returns {Promise<void>}
 */
async function updateAirSensors(data) {
    // ...
}
```

### SQL

- Ключевые слова в верхнем регистре
- Отступы для вложенных запросов

```sql
SELECT u.username, r.name AS role_name
FROM users u
JOIN roles r ON u.role_id = r.id
WHERE u.is_active = true;
```

## 📌 Структура коммитов

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>(<scope>): <description>

[optional body]

[optional footer]
```

### Типы коммитов

| Тип | Описание |
|-----|----------|
| `feat` | Новая функция |
| `fix` | Исправление ошибки |
| `docs` | Изменение документации |
| `style` | Форматирование, отступы |
| `refactor` | Рефакторинг без изменений функциональности |
| `test` | Добавление тестов |
| `chore` | Изменение конфигурации, утилиты |

### Примеры

```bash
feat(backend): добавить эндпоинт для управления дронами

fix(frontend): исправить отображение графиков в Safari

docs(readme): обновить инструкцию по установке

refactor(api): оптимизировать запросы к базе данных

test(backend): добавить тесты для auth модуля
```

## 🔀 Pull Request

### Перед созданием PR

1. Убедитесь, что код проходит тесты:
```bash
cd backend
pytest --cov=app
```

2. Проверьте код на соответствие стандартам:
```bash
# Python
flake8 app/
black --check app/

# JavaScript (если используется eslint)
npx eslint js/
```

3. Обновите документацию при необходимости

4. Добавьте тесты для новых функций

### Создание PR

1. Запушьте изменения:
```bash
git push origin feature/your-feature-name
```

2. Создайте Pull Request на GitHub

3. Заполните описание PR:
   - Что изменено
   - Почему это нужно
   - Как тестировать

### Шаблон PR

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] Новая функция
- [ ] Исправление ошибки
- [ ] Рефакторинг
- [ ] Документация

## Тестирование
Опишите, как тестировать изменения

## Чеклист
- [ ] Код соответствует стандартам проекта
- [ ] Добавлены тесты (если применимо)
- [ ] Обновлена документация
- [ ] Все тесты проходят
```

## 🧪 Тестирование

### Backend тесты

```bash
cd backend

# Все тесты
pytest

# С coverage
pytest --cov=app --cov-report=html

# Конкретный файл
pytest tests/test_api.py -v

# С логированием
pytest -v -s
```

### Frontend тестирование

1. Откройте DevTools в браузере
2. Проверьте консоль на ошибки
3. Протестируйте функциональность вручную

## 📞 Контакты

- Issues: [GitHub Issues](https://github.com/ghouse/ghouse/issues)
- Обсуждения: [GitHub Discussions](https://github.com/ghouse/ghouse/discussions)

---

Спасибо за ваш вклад! 🌿
