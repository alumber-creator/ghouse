# GHouse Dashboard | Автономный Агрокомплекс

Веб-дашборд для управления высокотехнологичной системой автономного агрокомплекса GHouse.

## 🌿 Описание

Современный минималистичный интерфейс в стиле "High-tech Eco" для мониторинга и управления:
- Умными теплицами (полив, освещение, вентиляция)
- Мониторингом параметров воздуха
- Беспилотниками (дронами)
- Ленточным конвейером
- Аналитикой почвы
- Telegram-ботом для уведомлений

## 📁 Структура проекта

```
ghouse-dashboard/
├── index.html              # Главная страница дашборда
├── login.html              # Страница входа (аутентификация)
├── README.md               # Документация
├── css/
│   ├── main.css           # Основные стили и переменные
│   ├── components.css     # Стили компонентов
│   └── widgets.css        # Стили виджетов
├── js/
│   ├── app.js             # Главное приложение
│   └── modules/
│       ├── api.js             # API клиент для backend
│       ├── auth.js            # Модуль аутентификации (JWT)
│       ├── websocket.js       # WebSocket для realtime обновлений
│       ├── greenhouse.js      # Модуль теплицы
│       ├── air-monitoring.js  # Мониторинг воздуха
│       ├── drones.js          # Модуль дронов
│       ├── conveyor.js        # Модуль конвейера
│       ├── soil.js            # Аналитика почвы
│       └── telegram.js        # Telegram интеграция
└── assets/
    ├── images/
    └── icons/
```

## 🎨 Дизайн-система

### Цветовая палитра
| Цвет | Значение | Hex |
|------|----------|-----|
| Background Primary | Основной фон | #1a1a1a |
| Background Secondary | Вторичный фон | #242424 |
| Background Card | Фон карточек | #2d2d2d |
| Accent Green | Акцентный зеленый | #00ff88 |
| Status Success | Успех | #00ff88 |
| Status Warning | Предупреждение | #ffaa00 |
| Status Error | Ошибка | #ff4444 |
| Status Info | Информация | #00ccff |

### Типографика
- Шрифт: 'Segoe UI', 'Roboto', 'Oxygen', sans-serif
- Заголовки: 1.1rem - 1.5rem
- Основной текст: 0.85rem - 0.95rem

## 🚀 Быстрый старт

### Требования
- Современный браузер (Chrome 90+, Firefox 88+, Safari 14+, Edge 90+)
- Backend сервер GHouse (запущен на порту 8000)

### Запуск

1. **Открыть страницу входа**
   ```
   Откройте login.html в браузере
   ```

2. **Войти в систему**
   - Логин: `admin` (или другой пользователь)
   - Пароль: ваш пароль

3. **После входа** откроется главная панель управления

### Локальный сервер (опционально)

```bash
# Python
python -m http.server 8000

# Node.js (http-server)
npx http-server -p 8000

# Затем откройте http://localhost:8000/login.html
```

## 🔐 Аутентификация

### JWT Token
- Access токен хранится в localStorage
- Автоматическое обновление токена
- Session timeout: 1 час

### Роли пользователей
| Роль | Уровень | Права |
|------|---------|-------|
| admin | 4 | Полный доступ |
| operator | 3 | Управление системами |
| viewer | 2 | Только просмотр |
| maintenance | 1 | Техническое обслуживание |

## 🔌 Интеграция с Backend API

### API Client (api.js)

Базовый URL API определяется автоматически:
- Development: `http://localhost:8000/api/v1`
- Production: `/api/v1` (relative path)

### Основные endpoints

#### Аутентификация
```javascript
// Вход
await ApiClient.login(username, password)

// Выход
await ApiClient.logout()

// Текущий пользователь
const user = await ApiClient.getCurrentUser()
```

#### Теплица
```javascript
const status = await ApiClient.getGreenhouseStatus()
await ApiClient.controlWatering(75)  // 0-100%
await ApiClient.controlLighting(80)  // 0-100%
await ApiClient.controlVentilation(45) // 0-100%
```

#### Воздух
```javascript
const current = await ApiClient.getAirCurrent()
const history = await ApiClient.getAirHistory({ from: '2026-02-18', to: '2026-02-18' })
const alerts = await ApiClient.getAirAlerts()
```

#### Дроны
```javascript
const drones = await ApiClient.getDrones()
await ApiClient.sendDroneCommand(1, 'takeoff')
await ApiClient.returnDroneToBase(1)
```

#### Конвейер
```javascript
const status = await ApiClient.getConveyorStatus()
await ApiClient.startConveyor()
await ApiClient.stopConveyor()
await ApiClient.setConveyorSpeed(1.5) // м/с
```

#### Почва
```javascript
const analysis = await ApiClient.getSoilCurrent()
await ApiClient.analyzeSoil()
const recommendations = await ApiClient.getSoilRecommendations()
```

#### Telegram
```javascript
const status = await ApiClient.getTelegramStatus()
await ApiClient.sendTelegramMessage('Тестовое сообщение')
await ApiClient.sendTelegramBroadcast('Рассылка всем')
```

### WebSocket (websocket.js)

Realtime обновления через WebSocket соединение.

#### Каналы
- `greenhouse` - События теплицы
- `air` - События воздуха
- `drones` - События дронов
- `conveyor` - События конвейера
- `soil` - События почвы
- `alerts` - Системные алерты
- `notifications` - Уведомления

#### Подписка на события
```javascript
WebSocketModule.onChannel('air', (message) => {
    console.log('Событие воздуха:', message)
})

WebSocketModule.onChannel('alerts', (message) => {
    console.log('Алерт:', message)
})
```

## 📊 Модули

### 1. Умная теплица (`greenhouse.js`)
- Управление поливом (0-100%)
- Управление освещением (0-100%)
- Управление вентиляцией (0-100%)
- Интеграция с API: `/api/v1/greenhouse/*`
- WebSocket канал: `greenhouse`

### 2. Мониторинг воздуха (`air-monitoring.js`)
- Температура (°C)
- Влажность (%)
- CO₂ (ppm)
- Давление (мм рт.ст.)
- Графики изменений (Chart.js)
- Система алертов
- Интеграция с API: `/api/v1/air/*`
- WebSocket канал: `air`

### 3. Дроны (`drones.js`)
- Мониторинг заряда аккумуляторов
- GPS координаты
- Статус модулей (захват, орошение, забор почвы)
- Зарядные станции
- Смена модулей
- Интеграция с API: `/api/v1/drones/*`
- WebSocket канал: `drones`

### 4. Конвейер (`conveyor.js`)
- Управление скоростью
- Статистика транспортировки
- Анимация работы
- Учет времени работы
- Интеграция с API: `/api/v1/conveyor/*`
- WebSocket канал: `conveyor`

### 5. Аналитика почвы (`soil.js`)
- Влажность почвы (%)
- pH уровень
- NPK анализ (Азот, Фосфор, Калий)
- История анализов
- Рекомендации
- Интеграция с API: `/api/v1/soil/*`
- WebSocket канал: `soil`

### 6. Telegram интеграция (`telegram.js`)
- Подключение бота через backend
- Отправка уведомлений
- Лог событий
- Ежедневные отчеты
- Интеграция с API: `/api/v1/telegram/*`
- WebSocket канал: `notifications`

## 🔄 Обновление данных

### WebSocket (приоритет)
- Realtime обновления при подключенном WebSocket
- Автоматическая переподключение при обрыве

### Polling (fallback)
- Периодический опрос API каждые 5-10 секунд
- Автоматическое переключение при недоступности WebSocket

## 📱 Адаптивность

Дашборд адаптируется под следующие разрешения:
- Desktop: > 1400px (4 колонки)
- Tablet: 900px - 1400px (2 колонки)
- Mobile: < 900px (1 колонка)

## 🔧 Конфигурация

### Переключение API/эмуляция
В каждом модуле есть флаг `useApi`:
```javascript
// В модулях по умолчанию true
const config = {
    useApi: true  // false для эмуляции
}
```

### Переменные окружения (для production)
```javascript
// В api.js автоматически определяется:
const baseUrl = window.location.hostname === 'localhost' 
    ? 'http://localhost:8000/api/v1' 
    : '/api/v1'
```

## 🛠️ Расширение функционала

### Добавление нового модуля
1. Создайте файл `js/modules/new-module.js`
2. Реализуйте паттерн модуля:
```javascript
const NewModule = (function() {
    const config = {
        apiEndpoints: {
            list: '/new-module',
        },
        useApi: true
    };
    
    let state = {};
    
    async function init() {
        if (config.useApi && window.ApiClient) {
            await loadFromApi();
        } else {
            loadState();
        }
        bindEvents();
        updateUI();
    }
    
    async function loadFromApi() {
        const data = await window.ApiClient.request(config.apiEndpoints.list);
        state = { ...state, ...data };
        saveState();
    }
    
    function handleWebSocketEvent(message) {
        // Обработка WebSocket событий
    }
    
    async function refreshData() {
        await loadFromApi();
        updateUI();
    }
    
    return {
        init,
        handleWebSocketEvent,
        refreshData,
        getState: () => ({ ...state })
    };
})();

window.NewModule = NewModule;
```
3. Подключите в `index.html`
4. Вызовите `NewModule.init()` в `app.js`

### Добавление новой секции
1. Добавьте пункт меню в `.nav-menu`
2. Создайте `<section id="new-section">` в `index.html`
3. Добавьте стили в CSS файлы
4. Обработайте навигацию в `app.js`

## 🐛 Отладка

### Консоль браузера
```javascript
console.log('[ModuleName] Сообщение');
```

### Логирование API
Все API запросы логируются в консоль:
```
[ApiClient] Инициализирован, baseUrl: http://localhost:8000/api/v1
[GreenhouseModule] Команда отправлена: watering = 75%
```

### Логирование WebSocket
```
[WebSocket] Соединение установлено
[WebSocket] Получено сообщение: { channel: 'air', type: 'sensor_update', data: {...} }
```

## 📦 Экспорт данных

Каждый модуль поддерживает экспорт данных в JSON:
- Программно: `ModuleName.exportData()`
- Через кнопки в интерфейсе (если предусмотрены)

## 🔐 Безопасность

### Реализовано
- JWT аутентификация (access + refresh токены)
- Автоматическое обновление токена
- RBAC (роли: admin, operator, viewer, maintenance)
- HTTPS (в production через backend)
- Аудит всех критических операций (через backend)

### Рекомендации для production
1. Включить HTTPS
2. Настроить CORS на backend
3. Использовать secure cookies для токенов
4. Включить rate limiting на backend
5. Регулярно обновлять зависимости

## 📈 Мониторинг

### Статус соединения
- WebSocket: индикатор в sidebar
- Polling: fallback режим
- Автоматическое переключение

### Health checks
- Проверка сессии каждые 30 секунд
- Проверка необходимости обновления токена
- Мониторинг подключения WebSocket

## 🤝 Поддержка

Для вопросов и предложений используйте систему issue tracker.

---

**GHouse Dashboard v2.0.0** | © 2026 Автономный Агрокомплекс
