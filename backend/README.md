# GHouse Backend API

Серверная часть системы управления автономным агрокомплексом GHouse.

## 🚀 Быстрый старт

### Требования
- Python 3.11+
- PostgreSQL 15+
- Redis 7+
- Docker и Docker Compose (опционально)

### Установка через Docker Compose

```bash
# Запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f api

# Остановка
docker-compose down
```

### Ручная установка

1. **Установка зависимостей**
```bash
pip install -r requirements.txt
```

2. **Настройка окружения**
```bash
cp .env.example .env
# Отредактируйте .env, указав ваши параметры
```

3. **Инициализация базы данных**
```bash
# PostgreSQL будет инициализирован автоматически при первом запуске
# или выполните миграции вручную:
psql -U ghouse -d ghouse -f migrations/001_init.sql
```

4. **Запуск сервера**
```bash
# Development режим
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# Production режим
uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
```

## 📁 Структура проекта

```
backend/
├── app/
│   ├── api/                  # API роуты
│   │   ├── auth.py          # Аутентификация
│   │   ├── greenhouse.py    # Теплица
│   │   ├── air.py           # Мониторинг воздуха
│   │   ├── drones.py        # Дроны
│   │   ├── conveyor.py      # Конвейер
│   │   ├── soil.py          # Почва
│   │   ├── telegram.py      # Telegram
│   │   ├── users.py         # Пользователи
│   │   ├── notifications.py # Уведомления
│   │   └── system.py        # Системные endpoints
│   ├── db/
│   │   └── database.py      # Подключение к БД
│   ├── models/
│   │   └── models.py        # SQLAlchemy модели
│   ├── schemas/
│   │   └── schemas.py       # Pydantic схемы
│   ├── services/
│   │   ├── websocket.py     # WebSocket сервис
│   │   ├── mqtt.py          # MQTT сервис
│   │   └── telegram.py      # Telegram сервис
│   ├── utils/
│   │   ├── auth.py          # Утилиты аутентификации
│   │   └── logging.py       # Настройка логирования
│   ├── config.py            # Конфигурация
│   └── main.py              # Главное приложение
├── migrations/
│   └── 001_init.sql         # SQL миграции
├── tests/
│   └── test_api.py          # Тесты API
├── docker-compose.yml       # Docker Compose конфигурация
├── Dockerfile               # Docker образ
├── requirements.txt         # Python зависимости
└── .env.example            # Пример переменных окружения
```

## 📡 API Endpoints

### Аутентификация
| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/api/v1/auth/login` | Вход |
| POST | `/api/v1/auth/logout` | Выход |
| POST | `/api/v1/auth/refresh` | Обновление токена |
| GET | `/api/v1/auth/me` | Текущий пользователь |

### Теплица
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/v1/greenhouse/status` | Статус систем |
| GET | `/api/v1/greenhouse/settings` | Настройки |
| PUT | `/api/v1/greenhouse/settings` | Обновление настроек |
| POST | `/api/v1/greenhouse/watering` | Управление поливом |
| POST | `/api/v1/greenhouse/lighting` | Управление освещением |
| POST | `/api/v1/greenhouse/ventilation` | Управление вентиляцией |

### Мониторинг воздуха
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/v1/air/current` | Текущие показатели |
| GET | `/api/v1/air/history` | История |
| GET | `/api/v1/air/thresholds` | Пороги |
| GET | `/api/v1/air/alerts` | Алерты |

### Дроны
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/v1/drones` | Список дронов |
| GET | `/api/v1/drones/{id}` | Информация о дроне |
| POST | `/api/v1/drones/{id}/command` | Команда дрону |
| POST | `/api/v1/drones/{id}/return-to-base` | Возврат на базу |

### Конвейер
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/v1/conveyor/status` | Статус |
| POST | `/api/v1/conveyor/start` | Запуск |
| POST | `/api/v1/conveyor/stop` | Остановка |
| PUT | `/api/v1/conveyor/speed` | Скорость |

### Почва
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/v1/soil/current` | Текущий анализ |
| POST | `/api/v1/soil/analyze` | Запуск анализа |
| GET | `/api/v1/soil/zones` | Зоны |
| GET | `/api/v1/soil/recommendations` | Рекомендации |

### Telegram
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/v1/telegram/status` | Статус |
| POST | `/api/v1/telegram/send` | Отправка сообщения |
| POST | `/api/v1/telegram/broadcast` | Рассылка |

### WebSocket
```
ws://localhost:8000/ws?token=<access_token>
```

**Каналы:**
- `greenhouse` - События теплицы
- `air` - События воздуха
- `drones` - События дронов
- `conveyor` - События конвейера
- `soil` - События почвы
- `alerts` - Системные алерты
- `notifications` - Уведомления

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `APP_ENV` | Окружение (development/production) | `development` |
| `APP_DEBUG` | Режим отладки | `true` |
| `DATABASE_URL` | URL подключения к PostgreSQL | `postgresql://...` |
| `REDIS_URL` | URL подключения к Redis | `redis://localhost:6379` |
| `JWT_SECRET_KEY` | Секретный ключ JWT | `change_me` |
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | - |
| `MQTT_BROKER` | MQTT брокер | `localhost` |

## 🧪 Тестирование

```bash
# Запуск всех тестов
pytest

# Запуск с coverage
pytest --cov=app

# Запуск конкретных тестов
pytest tests/test_api.py -v
```

## 📊 Мониторинг

### Prometheus метрики
Доступны по адресу: `http://localhost:8000/metrics`

### Health checks
- Liveness: `GET /api/v1/health`
- Readiness: `GET /api/v1/health`

## 🐳 Docker

### Сборка образа
```bash
docker build -t ghouse-backend .
```

### Запуск контейнера
```bash
docker run -d \
  -p 8000:8000 \
  -e DATABASE_URL=postgresql://... \
  -e REDIS_URL=redis://... \
  ghouse-backend
```

## 🔐 Безопасность

- JWT аутентификация (access + refresh токены)
- RBAC (роли: admin, operator, viewer, maintenance)
- HTTPS (в production)
- Rate limiting (100 запросов/мин)
- Аудит всех критических операций

## 📝 Лицензия

© 2026 GHouse. Все права защищены.
